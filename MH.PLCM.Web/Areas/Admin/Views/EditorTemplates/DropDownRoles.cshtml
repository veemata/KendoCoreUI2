@model ApplicationRoleViewModel
@(Html.Kendo().DropDownListFor(m => m.ApplicationRoleId)
        .DataValueField("ApplicationRoleId")
        .DataTextField("ApplicationRoleName")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetRolesList", "UserInRoles");
        })
        .ServerFiltering(false);
    })
)
